<h1>
    <%=@recipe.name%>
</h1>

<ul>
    <%@recipe.ingredients.each do |ingredient|%>
        <li>
            <%=ingredient.name%>

            <%="Quantity" + String(@details[0].quantity) + " " + @details[0].quantity_unit%>
        </li>
    <%end%>
</ul>